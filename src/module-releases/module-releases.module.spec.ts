import { ModuleReleasesModule } from './module-releases.module';
describe('ModuleReleasesModule', () => {
  it('should be defined', () => {
    expect(new ModuleReleasesModule()).toBeDefined();
  });
});

